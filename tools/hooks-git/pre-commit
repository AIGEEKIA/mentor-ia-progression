#!/bin/sh

echo "🔍 Vérification des standards Mentor IA..."

# Couleurs pour l'affichage
RED='\033[0;31m'
GREEN='\033[0;32m'
YELLOW='\033[1;33m'
BLUE='\033[0;34m'
NC='\033[0m' # No Color

# Fonction pour afficher les messages
print_status() {
    echo -e "${BLUE}[INFO]${NC} $1"
}

print_success() {
    echo -e "${GREEN}[SUCCESS]${NC} $1"
}

print_warning() {
    echo -e "${YELLOW}[WARNING]${NC} $1"
}

print_error() {
    echo -e "${RED}[ERROR]${NC} $1"
}

# Vérifier les IDs explicites
print_status "Vérification des IDs explicites..."

if git diff --cached --name-only | grep -E '\.(tsx|jsx|ts|js)$' | xargs grep -l "id=\"[a-z0-9]*\"" 2>/dev/null; then
    print_error "❌ IDs génériques détectés. Utilisez des noms explicites."
    print_error "Exemple : id=\"user-profile-edit-button\" au lieu de id=\"btn1\""
    exit 1
fi

print_success "✅ IDs explicites respectés"

# Vérifier les data-testid
print_status "Vérification des data-testid..."

if git diff --cached --name-only | grep -E '\.(tsx|jsx|ts|js)$' | xargs grep -l "data-testid=" 2>/dev/null; then
    print_success "✅ data-testid présents"
else
    print_warning "⚠️  Aucun data-testid détecté. Assurez-vous d'en ajouter pour les tests."
fi

# Vérifier les attributs ARIA
print_status "Vérification des attributs ARIA..."

if git diff --cached --name-only | grep -E '\.(tsx|jsx|ts|js)$' | xargs grep -l "aria-label\|aria-expanded\|aria-controls\|role=" 2>/dev/null; then
    print_success "✅ Attributs ARIA présents"
else
    print_warning "⚠️  Aucun attribut ARIA détecté. Améliorez l'accessibilité."
fi

# Vérifier les images avec alt
print_status "Vérification des images avec alt..."

if git diff --cached --name-only | grep -E '\.(tsx|jsx|ts|js)$' | xargs grep -l "<img" 2>/dev/null | xargs grep -L "alt=" 2>/dev/null; then
    print_error "❌ Images sans attribut alt détectées. Ajoutez des descriptions."
    exit 1
fi

print_success "✅ Images avec alt respectées"

# Vérifier les liens avec aria-label ou contenu
print_status "Vérification des liens accessibles..."

if git diff --cached --name-only | grep -E '\.(tsx|jsx|ts|js)$' | xargs grep -l "<a" 2>/dev/null | xargs grep -L "aria-label\|>[^<]*[a-zA-Z]" 2>/dev/null; then
    print_warning "⚠️  Liens sans contenu ou aria-label détectés."
fi

# Vérifier les boutons avec aria-label
print_status "Vérification des boutons accessibles..."

if git diff --cached --name-only | grep -E '\.(tsx|jsx|ts|js)$' | xargs grep -l "<button" 2>/dev/null | xargs grep -L "aria-label\|>[^<]*[a-zA-Z]" 2>/dev/null; then
    print_warning "⚠️  Boutons sans aria-label ou contenu détectés."
fi

# Vérifier les console.log en production
print_status "Vérification des console.log..."

if git diff --cached --name-only | grep -E '\.(tsx|jsx|ts|js)$' | xargs grep -l "console\.log" 2>/dev/null; then
    print_warning "⚠️  console.log détectés. Supprimez-les en production."
fi

# Vérifier les TODO et FIXME
print_status "Vérification des TODO et FIXME..."

if git diff --cached --name-only | grep -E '\.(tsx|jsx|ts|js)$' | xargs grep -l "TODO\|FIXME" 2>/dev/null; then
    print_warning "⚠️  TODO ou FIXME détectés. Traitez-les avant le commit."
fi

# Vérifier la longueur des lignes
print_status "Vérification de la longueur des lignes..."

if git diff --cached --name-only | grep -E '\.(tsx|jsx|ts|js)$' | xargs grep -l ".\{100,\}" 2>/dev/null; then
    print_warning "⚠️  Lignes trop longues détectées (>100 caractères)."
fi

print_success "✅ Standards Mentor IA respectés !"
echo ""
echo "🎯 Commit autorisé !"
echo "📝 N'oubliez pas de :"
echo "   - Tester votre code"
echo "   - Documenter les changements"
echo "   - Créer du contenu YouTube"
echo "   - Mesurer vos progrès"
echo "" 